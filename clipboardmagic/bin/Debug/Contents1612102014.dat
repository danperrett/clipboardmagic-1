messageCounter.LTECount[str]

messageCounter.variableToCountTo

if (messageCounter.variableToCountTo.ContainsKey(str))
                            {
                                vars.SetVariableByName(messageCounter.variableToCountTo[str], messageCounter.LTECount[str]);
                            }

resetMessageCountList

scriptedModeInactivityTimerSet

resetMessageCountList

LogAndTraceRTTMessageCount

 messageCounter.RttCountMessage = true;

