 if(uploadPeriod == UploadPeriod.WhenCoppied)
                    {
                        lock (backupToServer)
                        {
                            sendtoServer(text);
                            backupToServer = "";
                        }
                    }

backupToServer 

UploadPeriod

